# SimpleAPI library

## Getting started

```bash
git clone <actual URL>
cd simpleapi_library
mkdir -p <build_dir> && cd $_
cmake ../
make -j$(nproc)
```

## Description
TODO

## Usage
Use `<build_dir>/libs/SimpleAPI.so` and `<build_dir/libs/include/*.h>` in your project.