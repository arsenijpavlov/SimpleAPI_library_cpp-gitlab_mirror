## FIXME: (перепроверить) Логика парсинга комментариев

Если на строке значения есть комментарии (в случае многострочного документа), то  пока не встретится новая пара ключ-значение первый встречный комментарий считается завершающим к предыдущей паре ключ-значение; все последующие комментарии до новой пары считаются одним комплексным комментарием к новой паре

Принципиальная схема: "[P]{k:v},[S][P][...][P]{k:v}...", где:
* [P] - Preview Comment
* [S] - Suffix Comment
* {k:v} - пара значений ключ:значение

Перенос строки обязательно означает, что [S] для предыдущего значения не было


### FIXME: (перепроверить) Array
* разделители: перенос строки, запятая
 - перенос строки можно считать страховкой, так как базовый синтаксис JSON такой разделитель не нарушает

### FIXME: (перепроверить) parse JSON
- ключ с пустым значением заполняется null-элементом

### FIXME: (перепроверить) parse JSON Array
- пустое значение между разделителями не создаёт null-элементы

---

### Парсинг
* в случае ошибки синтаксиса не генерируется исключение - на выходе получаем только корректно обработанные данные
* в случае ошибки парсинга методы (первого уровня) Config::parse*() вернут false, при этом заполнив опциональную строковую переменную Config::m_error
* в случае ошибки парсинга методы (второго уровня) IElementContainer::parse*() вернут строку ошибки

