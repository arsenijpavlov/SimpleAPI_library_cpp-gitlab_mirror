### ToComment()
- корневой элемент управляет оформлением комментариев при записи
---

### FromComment()
* однострочный комментарий:
    - может обрамляться 1 или 2 символами
    - если второй символ в строке пробел, то в *CommentDesign* запишется первый элемент и 0
* многострочный комментарий:
    - может обрамляться 1 или 2 символами (3 символ указывается как замена 1 при закрытии блока комментариев)
    - правильную границу комментария определяет функция чтения конфига
    - здесь проверка на существование границы в конце будет опускаться

При парсинге заполняется CommentDesign::opt_multiline_column_size. Всегда.

### Сравнение стилей комментариев
Следует держать в уме тот факт, что нельзя однозначно сравнить два CommentDesign с разыми opt_multiline_column_size
Проблема исходит из того, что при попытке записать такой стиль на комментарий, который как самую максимальную
строку принимает, например N-1 (N - ширина колонки), то следующий парсер, который будет читать из этого результата
в своём результате увидит ширину колонки как N-1

---

### Логика парсинга комментариев
* если на строке значения есть комментарии (в случае многострочного документа), то 
пока не встретится новая пара ключ-значение первый встречный комментарий считается завершающим 
к предыдущей паре ключ-значение; все последующие комментарии до новой пары считаются одним 
комплексным комментарием к новой паре

Принципиальная схема: "[P]{k:v},[S][P][...][P]{k:v}...", где:
* [P] - Preview Comment
* [S] - Suffix Comment
* {k:v} - пара значений ключ:значение

Перенос строки обязательно означает, что [S] для предыдущего значения не было

---

### NOTE:
* названия функций с подчёркиванием использовать только для контейнеров(?)
* пользовательские границы комментариев:
 - std::vector<std::array<uint8_t, 2>> user_oneline_comment_braces;
 - std::vector<std::array<uint8_t, 3>> user_multiline_comment_braces;
* Универсальная ссылка - сама по себе константа, квалификатор не нужен

---

### Array
* разделители: перенос строки, запятая
 - перенос строки можно считать страховкой, так как базовый синтаксис JSON такой разделитель не нарушает

### parse JSON
- ключ с пустым значением заполняется null-элементом

### parse JSON Array
- пустое значение между разделителями не создаёт null-элементы

---

### findPkg

sudo apt-get install pkg-config

---

### Использование библиотеки
Предполагается, что на один бинарник существует только одно подключение к SimpleAPI (неважно: статика или динамика)
