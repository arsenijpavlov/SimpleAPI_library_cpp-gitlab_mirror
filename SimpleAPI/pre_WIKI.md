## ToComment()
- корневой элемент управляет оформлением комментариев при записи

---

## FromComment()
* однострочный комментарий:
    - может обрамляться 1 или 2 символами
    - если второй символ в строке пробел, то в *CommentDesign* запишется первый элемент и 0
* многострочный комментарий:
    - может обрамляться 1 или 2 символами (3 символ указывается как замена 1 при закрытии блока комментариев)
    - правильную границу комментария определяет функция чтения конфига
    - здесь проверка на существование границы в конце будет опускаться

---

# TODO: 
* рантайм-замена символов, после которых можно сделать перенос строки в комментариях

---

# NOTE:
* названия функций с подчёркиванием использовать только для контейнеров(?)
* пользовательские границы комментариев:
 - std::vector<std::array<uint8_t, 2>> user_oneline_comment_braces;
 - std::vector<std::array<uint8_t, 3>> user_multiline_comment_braces;
* Универсальная ссылка - сама по себе константа, квалификатор не нужен

---

# Array
* разделители: перенос строки, запятая
 - перенос строки можно считать страховкой, так как базовый синтаксис JSON такой разделитель не нарушает
